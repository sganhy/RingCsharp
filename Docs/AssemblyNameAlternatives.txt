Name			Angle
=============================================================================
Apex			Top of the stack — from schema to API in one layer
Forge			Still valid — now forging APIs as well as schemas
Aether			Invisible infrastructure layer that everything runs on
Praxis			Greek for "doing" — turns definitions into working systems
Lattice			The structural grid connecting data model to API surface
Verve			Lightweight, energetic — a fast runtime that generates live APIs
Axiom			You define the rules, it generates everything else (* chosen by Claude 4.6)
Conduit			Data flows from config through ORM out to REST endpoints
Blueprint		You provide the blueprint, it builds the system

Telos * 		(Greek: purpose/end) schema with intent
Logaxis			Logical axis — the logical axis around which endpoints rotate
Dynaxis			Dynamic axis — the live pivot between DB and API
Physema			Physical schema compressed into a name

Cambium			The living generative layer in a tree between bark and wood — it continuously produces new structure without killing the tree. Maps perfectly to zero-downtime schema evolution
Velum * 		A thin membranous covering layer — precise, anatomical, unusual

Voussoir * 		The wedge stones that form an arch — each piece is load-bearing, together they span a gap (physical → logical)
Spandrel * 		The intermediary space between an arch and its frame — exactly what this assembly occupies
Impost * 		The block that transfers arch load to a column — a pure transfer/translation layer


Ostinato * 		A persistently repeated pattern beneath changing melody — the stable schema engine beneath evolving endpoints
Athanor * 		A self-feeding furnace that maintains constant heat indefinitely — a direct metaphor for zero-downtime continuous operation
Continuo * 		Basso continuo — the continuous harmonic foundation underlying baroque composition, always present, never stopping
Velum * 		A thin membranous covering layer — precise, anatomical, unusual


* doesn't exist



Alchemy, with real semantic fit:

Amalgam * — an alloy formed by fusing distinct elements into one; a schema framework that unifies tables, fields, relations, indexes, and constraints into one coherent structure is literally an amalgam. Strong, plain-English-adjacent (not too obscure), short.
Alkahest * — the legendary universal solvent, alchemy's answer to "dissolves anything." A nice metaphor for a driver meant to be a universal, dependency-free conduit to the database. More obscure, harder to say at first glance, but distinctive and unlikely to collide with anything.
Athanor — the alchemist's furnace, used for slow transformation over sustained heat. Fits the "built from scratch over a long-running project" story, if you want the name to reference the effort rather than the artifact.


Geology, with real semantic fit:

Strata * — layered structure, directly mirroring the meta-model you've actually built (Table → Field/Relation/Index/View, Schema above them). Probably the single most literal match to what the code does.
Aquifer — an underground layer that stores and transmits water. Good driver metaphor (stores and moves data), reads as deliberate rather than decorative.
Karst — a landscape carved by dissolution into underground channels and caverns; nice metaphor for connection pooling/routing, more obscure and distinctive, less immediately legible to someone with no geology background.


Geology — same vein as before, further from the well-picked-over words:

Xenolith — a fragment of foreign rock embedded inside another rock. Genuinely literal fit: this project embeds foreign resource data inside the assembly via GetManifestResourceStream.
Breccia — rock made of broken fragments cemented into one mass by a fine matrix. Fits a schema built from many small pieces (tables, fields, relations) bound into a whole.
Gossan — weathered, oxidized rock at the surface that signals an ore deposit below. Fits a metadata layer that points to the real data underneath it.
Saprolite — rock chemically weathered in place, still preserving the original rock's structure. Fits a layer that wraps Postgres while preserving its underlying shape.

Alchemy — rarer than Alembic/Alkahest/Athanor:
5. Cucurbit — the gourd-shaped vessel forming the body of an alembic still. Alchemy-rooted, without Alembic's exact-name collision.
6. Menstruum — a dissolving/extracting solvent, alkahest's lesser-known cousin. Same "universal connector" metaphor.
7. Calx — the ash residue left after calcination. Short and punchy, closer to "Ring"'s length than most of these.
8. Spagyric — an alchemical method of separating a substance into parts and recombining them. Fits "decompose into schema/meta, recombine into a working whole" well.

A different well entirely — structural/architectural terms, since "schema" itself means a structural framework:
9. Trestle — a framework of beams supporting a structure from below. Literal "framework," and outside the two domains you'd already mined.
10. Fascia — a band or layer that binds and supports what's beneath it (architecture and anatomy both use the word). Distinct sound, layered-support metaphor.



Name	Why it fits	Availability
Nexum * 	Latin for "link/bond" — what an ORM does: binds objects to the DB. Clean, short, memorable.
Velum	A thin, precise covering layer — the ORM/schema layer sitting over your raw database. Anatomically exact metaphor.
Continuo	The always-present harmonic foundation — the persistent schema runtime underlying your application.
Athanor	A self-sustaining furnace — a runtime that keeps schemas live indefinitely with zero downtime.
Cambium *	The living generative layer in a tree — continuously produces new structure, maps to schema evolution.

Voici 10 propositions :

Nom	Idée
Nexus	Un point central qui relie schéma, configuration, déploiement et API.
Forge	L'idée de fabriquer une base et ses services à partir d'une configuration.
Atlas	Évoque une structure complète et une vision globale de l'architecture.
Genesis	Le système est capable de créer une base et son environnement à partir de zéro.
Helix !	Évoque une structure organisée, évolutive et continue.
SchemaFlow	Très explicite : gestion et évolution du schéma comme un flux automatisé.
DataForge !	Combine clairement les notions de données et de génération/construction.
CoreDB	Positionne le projet comme le cœur de gestion de la base de données.
Continuum	Fait référence à la continuité, au déploiement et au business continuity.
Foundry	Une « fonderie » : un système qui produit et assemble automatiquement l'infrastructure.

from Gemini ==> 

1. Functional & Hybrid (ORM + API)

These names directly highlight the dual nature of your framework—seamlessly bridging persistent data storage with active REST delivery.

    DataBridge / DataBridge.NET: Clear and professional. Emphasizes bridging physical schema definition directly with active REST APIs.

    AutoSchema: Highlights the dynamic, declarative nature of creating constraints, tables, views, and live endpoints on the fly.

    SchemaRest: Simple, explicit, and descriptive. Directly communicates what the library does: turn schema configurations into REST resources.

    OrmApi / RestOrm: Direct and no-nonsense. Leaves zero doubt about the library's scope and capabilities.

2. Architectural & Dynamic (Hot-Swappable / Live)

These options focus on the ability to dynamic-link REST endpoints and update schemas without interrupting existing APIs.

    LiveSchema: Focuses on the real-time, non-blocking aspect of building tables and linking APIs without stopping running services.

    FlexSchema: Emphasizes adaptability and fluid reconfiguration of endpoints and underlying ORM schemas.

    Helix: Implies a continuous, connected structure—linking database models and REST endpoints seamlessly.

    AdaptiData: Highlights how easily the library adapts to new configuration files and live endpoint additions.

3. Abstract & Conceptual

Shorter, memorable names that sound like modern developer tooling or open-source libraries.

    Expanse: Suggests an expanding data layer where you can constantly add tables, views, and APIs on the fly.

    Nexus: Represents a central connection point where configuration, ORM models, tables, and REST endpoints meet.

    Conduit: Suggests a fluid pipeline flowing directly from database tables/views to external REST interfaces.

    Synthetix / SynthData: Plays on the idea of dynamically "synthesizing" database schemas and API layers from a single configuration file.
