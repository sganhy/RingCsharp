using Ring.Schema.Models;

namespace Ring.Schema.Builders
{
    internal sealed class RoleBuilder : EntityBuilder
    {

        internal Role GetInstance(long id, string name, string description, bool enabled, bool baseline, bool administrator)
        {
            return null;
        }

    }
}
